sub=crmservice
libdir=@libdir@
includedir=@includedir@/@PACKAGE_TARNAME@

ocfdir=@OCF_RA_DIR@

Name:             lib${sub}
URL:              @PACKAGE_URL@
Version:          @PACKAGE_VERSION@
Description:      Service APIs for Pacemaker
# pacemaker-service is not very useful without glib hash table and list APIs
Requires:         pacemaker glib-2.0
# @TODO Create equivalent of DBUS_LIBS and add here if HAVE_dbus is true
Requires.private: libxml-2.0
Conflicts:
Cflags:           -I${includedir}
Libs:             -L${libdir} -l${sub}
# @TODO Add -lrt if HAVE_SCHED_SETSCHEDULER is true
Libs.private:
